type Tstate = Date
type Taction = { type: string, payload: string }
export const initDate = new Date();

const reducer = (state: Date, action: Taction) => {
    switch(action.type){
        
    }
}

export default reducer;